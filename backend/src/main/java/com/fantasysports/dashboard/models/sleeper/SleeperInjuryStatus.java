package com.fantasy.transformer.models.sleeper;

public enum SleeperInjuryStatus {
    QUESTIONABLE, //injured (questionable will play)
    OUT, //injured
    IR, //injured (injured reserve)
    DOUBTFUL, //injured (doubtful will play)
    PUP, //injured (Physically Unable to Perform)
    SUS, //suspended
    NA //retiring/has retired
}
